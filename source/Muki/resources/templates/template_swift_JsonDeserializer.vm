/**
 * Generated by Muki $helper.getMukiVersion()
 * This class has been automatically generated from a template. 
 * Do not change it manually. Instead, change the model and run the generation process again!
 */
import Foundation

class JsonDeserializer: NSObject {

#foreach( $type in $definitions.model )
$helper.getJsonDeserializerDeserializeMethod( $type )

#end
        
    /**
     Private methods
     */
#foreach( $type in $definitions.model )
$helper.getJsonDeserializerExtractMethod( $type )

#end

    func toString(json: JSON) -> String {
        if let value = json.string {
            return value
        } else {
            return ""
        }
    }
    
    func toBool(json: JSON) -> Bool {
        if let value = json.bool {
            return value
        } else {
            return false
        }
    }
    
    func toInt(json: JSON) -> Int {
        if let value = json.int {
            return value
        } else {
            return 0
        }
    }
    
    func toInt64(json: JSON) -> Int64 {
        if let value = json.int64 {
            return value
        } else {
            return 0
        }
    }
    
    func toDouble(json: JSON) -> Double {
        if let value = json.double {
            return value
        } else {
            return 0.0
        }
    }
    
}
